# TMDB Movie Browser

Vite + React + TypeScript로 만든 TMDB 영화 검색 / 무한 스크롤 데모입니다.
TMDB API 를 사용했습니다.

## Scripts

- `npm install` : 의존성 설치
- `npm run dev` : 개발 서버 실행
- `npm run build` : 빌드
- `npm run preview` : 빌드 결과 미리보기

## Tech Stack

- React (TypeScript)
- Vite
- Axios
- TMDB API
- React Query
